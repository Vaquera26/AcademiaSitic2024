using System;
using System.Diagnostics.SymbolStore;

namespace Exercises
{
    internal class Program
    {

        /*
        // Metodos
        static void Main(string[] args)
        {

            int a = 5;
            int b = 10;  

            Console.WriteLine(SumInteger(out a, b));
            Console.WriteLine(a);
            Console.WriteLine(b);
            Console.ReadKey();

        }   
        
        // Valor por Referencia (ref), haga lo que haga 
        // Valor out, no importa si lo inicializas, en el ref tiene que estar instanciado atnes de qut lo envies

        public static int SumInteger(out int a, int b) 
        {

            a = b;
            return a + b;

        }

        */
    }
}
